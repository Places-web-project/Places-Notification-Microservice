# Places Notification Microservice

Spring Boot 3 microservice for feedback forms and email notifications (MailTrap).

## Local run

```bash
./mvnw spring-boot:run
```

Default port: `8083`

## Environment variables

See `.env.example`.
